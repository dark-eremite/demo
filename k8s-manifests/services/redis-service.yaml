apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  selector:
    app: redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: redis-port
  type: ClusterIP
  # Внутренний сервис - доступен только внутри кластера
