apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  selector:
    app: postgres
  ports:
  - protocol: TCP
    port: 5432
    targetPort: postgres-port
  type: ClusterIP
  # Внутренний сервис - доступен только внутри кластера
